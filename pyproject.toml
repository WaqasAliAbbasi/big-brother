[project]
name = "big-brother"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Waqas Ali", email = "waqas.abbasi@outlook.com" }
]
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.3.5",
    "ynab>=1.4.0",
]

[project.scripts]
big-brother = "big_brother:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "ruff>=0.11.8",
]
