services:
  big-brother:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PYTHONUNBUFFERED=1
      - SELENIUM_SERVER=http://selenium:4444
    env_file:
      - .env
    restart: always
    logging:
      options:
        max-size: 50m